{
  "name": "NuxtLoadLibraryOnClient",
  "version": "1.0.0",
  "engines": {
    "node": "10.0.0",
    "yarn": "1.6.0"
  },
  "description": "test to load objects in browser",
  "author": "mohrtw",
  "scripts": {
    "dev": "nuxt",
    "dev-debug": "node --inspect=3333 node_modules/.bin/nuxt",
    "dev-debug2": "nuxt build && nuxt start --inspect-brk=3333",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "nuxt": "^1.4.0"
  }
}
